export enum VALID_RESOURCES {
  typeProcedures = 'types-procedures',
  communication = 'communication',
  institutions = 'institutions',
  dependencies = 'dependencies',
  external = 'external',
  internal = 'internal',
  officers = 'officers',
  archived = 'archived',
  accounts = 'accounts',
  reports = 'reports',
  roles = 'roles',
  jobs = 'jobs',
}

export enum VALID_ACTIONS {
  applicant = 'applicant',
  search = 'search',
}
